<!--components/cart-bar/index.wxml-->
<view class="cart-bar {{itemCount > 0 ? 'active' : ''}}" bindtap="onTap">
  <view class="cart-icon-wrap">
    <view class="cart-emoji">🛒</view>
    <view class="badge" wx:if="{{itemCount > 0}}">{{itemCount}}</view>
  </view>
  <view class="total-text flex-1">
    <text wx:if="{{itemCount > 0}}">¥{{totalAmount/100}}</text>
    <text wx:else class="text-secondary text-sm">购物车空空如也</text>
  </view>
  <view class="checkout-btn" wx:if="{{itemCount > 0}}">
    {{totalAmount >= minOrder ? '去结算' : '差¥' + ((minOrder-totalAmount)/100).toFixed(2) + '起送'}}
  </view>
</view>
