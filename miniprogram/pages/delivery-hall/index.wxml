<!--pages/delivery-hall/index.wxml — 服务中心-->
<view class="services-page">
  <view class="services-header">
    <view class="header-title">服务中心</view>
  </view>

  <view class="services-grid">
    <view class="service-card" wx:for="{{services}}" wx:key="label"
      bindtap="onServiceTap" data-url="{{item.url}}" data-type="{{item.type}}">
      <view class="service-icon">{{item.icon}}</view>
      <view class="service-label">{{item.label}}</view>
    </view>
  </view>
</view>
